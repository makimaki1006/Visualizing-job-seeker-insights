# システムアーキテクチャ

ジョブメドレー求職者データ分析プロジェクトのアーキテクチャ詳細。

---

## 📐 システム全体図

```
┌─────────────────────────────────────────────────────────────┐
│                      ユーザー操作                            │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────────┐
│                  Googleスプレッドシート                       │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  メニューUI (MenuIntegration.gs:onOpen)                │  │
│  │  ├─ ⚡ 高速CSVインポート (Upload_Enhanced.html)         │  │
│  │  ├─ 🐍 Python連携 (PythonCSVImporter.gs)              │  │
│  │  ├─ 🗺️ 地図表示                                        │  │
│  │  ├─ 📈 統計分析・ペルソナ                              │  │
│  │  ├─ 🌊 フロー・移動パターン分析                        │  │
│  │  └─ データ管理                                         │  │
│  └────────────────────────────────────────────────────────┘  │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ (GAS処理)
       ▼
┌──────────────────────────────────────────────────────────────┐
│              GASスクリプト (Apps Script)                      │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  MenuIntegration.gs                                    │  │
│  │  ├─ showEnhancedUploadDialog() → Upload_Enhanced.html │  │
│  │  └─ importPythonCSVDialog()                           │  │
│  ├────────────────────────────────────────────────────────┤  │
│  │  PythonCSVImporter.gs                                  │  │
│  │  ├─ batchImportPythonResults()                        │  │
│  │  ├─ processCSVFile()                                  │  │
│  │  └─ processJSONFile()                                 │  │
│  ├────────────────────────────────────────────────────────┤  │
│  │  DataValidationEnhanced.gs                             │  │
│  │  ├─ validateDataTypes()                               │  │
│  │  ├─ validateCoordinates()                             │  │
│  │  ├─ validateColumnCount()                             │  │
│  │  ├─ detectDuplicateKeys()                             │  │
│  │  ├─ validateAggregation()                             │  │
│  │  ├─ validateForeignKeys()                             │  │
│  │  └─ validateWardLevelGranularity()                    │  │
│  ├────────────────────────────────────────────────────────┤  │
│  │  PersonaDifficultyChecker.gs                           │  │
│  │  ├─ getPersonaDataForDifficulty()                     │  │
│  │  ├─ calculateDifficultyScore()                        │  │
│  │  └─ filterPersonasByConditions()                      │  │
│  └────────────────────────────────────────────────────────┘  │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ (CSVインポート)
       ▼
┌──────────────────────────────────────────────────────────────┐
│              ファイルシステム (Windows)                       │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  job_medley_project/data/output/                       │  │
│  │  ├─ phase1/ (MapMetrics, Applicants, etc.)           │  │
│  │  ├─ phase2/ (ChiSquareTests, ANOVATests)             │  │
│  │  ├─ phase3/ (PersonaSummary, PersonaDetails)         │  │
│  │  ├─ phase6/ (MunicipalityFlowEdges, etc.)            │  │
│  │  └─ geocache.json                                     │  │
│  └────────────────────────────────────────────────────────┘  │
└──────▲───────────────────────────────────────────────────────┘
       │
       │ (CSVエクスポート)
       │
┌──────┴───────────────────────────────────────────────────────┐
│              Pythonスクリプト (ローカル実行)                  │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  run_complete.py (エントリーポイント)                  │  │
│  │  ├─ select_csv_file() → GUIファイル選択               │  │
│  │  └─ main()                                            │  │
│  │      ├─ AdvancedJobSeekerAnalyzer(filepath)           │  │
│  │      ├─ export_phase1_data()                          │  │
│  │      ├─ export_phase2_data()                          │  │
│  │      ├─ export_phase3_data(n_clusters=5)              │  │
│  │      └─ export_phase6_data()                          │  │
│  ├────────────────────────────────────────────────────────┤  │
│  │  test_phase6_temp.py (分析エンジン)                    │  │
│  │  ├─ class MasterData                                  │  │
│  │  └─ class AdvancedJobSeekerAnalyzer                   │  │
│  │      ├─ __init__() → データ読み込み・前処理           │  │
│  │      ├─ load_geocache() / save_geocache()             │  │
│  │      ├─ process_data()                                │  │
│  │      ├─ _extract_basic_info()                         │  │
│  │      ├─ _process_desired_work()                       │  │
│  │      ├─ _extract_municipality()                       │  │
│  │      ├─ _analyze_flow()                               │  │
│  │      ├─ _calculate_proximity()                        │  │
│  │      ├─ _proximity_analysis()                         │  │
│  │      ├─ export_phase1_data()                          │  │
│  │      ├─ export_phase2_data()                          │  │
│  │      ├─ export_phase3_data()                          │  │
│  │      └─ export_phase6_data()                          │  │
│  └────────────────────────────────────────────────────────┘  │
└──────▲───────────────────────────────────────────────────────┘
       │
       │ (入力データ読み込み)
       │
┌──────┴───────────────────────────────────────────────────────┐
│              入力CSVファイル (data/input/)                    │
│  ├─ 統合_求職者情報沖縄_介護 (1).csv                         │  │
│  ├─ 統合_求職者情報京都_介護 (1).csv                         │  │
│  ├─ 栃木_生活相談員 (1).csv                                  │  │
│  └─ 統合_求職者情報宮城_介護.csv                             │  │
└──────────────────────────────────────────────────────────────┘
       │
       │ (外部API)
       ▼
┌──────────────────────────────────────────────────────────────┐
│              Google Maps Geocoding API                        │
│  (キャッシュあり: geocache.json 1,901件)                      │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔗 依存関係マップ

### Pythonモジュール依存

```
run_complete.py
  │
  ├─ import: test_phase6_temp (AdvancedJobSeekerAnalyzer, select_csv_file)
  │
  └─ 呼び出し:
       ├─ select_csv_file() → tkinter.filedialog
       └─ AdvancedJobSeekerAnalyzer(filepath)
            │
            └─ test_phase6_temp.pyの全メソッド実行


test_phase6_temp.py
  │
  ├─ import: pandas, numpy, matplotlib, scikit-learn
  ├─ import: googlemaps (オプション)
  ├─ import: json, os, re, tkinter
  │
  └─ クラス:
       ├─ MasterData (マスターデータ管理)
       └─ AdvancedJobSeekerAnalyzer (分析エンジン)
            │
            ├─ 依存: geocache.json (読み込み・書き込み)
            ├─ 依存: 入力CSVファイル (data/input/)
            └─ 出力: Phase 1-6 CSVファイル


COMPREHENSIVE_TEST_SUITE.py
  │
  ├─ import: test_phase6_temp (AdvancedJobSeekerAnalyzer)
  ├─ import: unittest, json, os
  │
  └─ テストメソッド:
       ├─ run_regex_tests()
       ├─ run_data_generation_tests()
       ├─ run_gas_unit_tests()
       ├─ run_integration_tests()
       └─ run_e2e_tests()
```

---

### GASスクリプト依存

```
MenuIntegration.gs
  │
  ├─ 呼び出し: showEnhancedUploadDialog()
  │              └─ HTML表示: Upload_Enhanced.html
  │
  ├─ 呼び出し: importPythonCSVDialog()
  │              └─ 関数呼び出し: PythonCSVImporter.batchImportPythonResults()
  │
  └─ トリガー: onOpen() (スプレッドシート起動時)


PythonCSVImporter.gs
  │
  ├─ 依存: DriveApp (Google Drive API)
  ├─ 依存: SpreadsheetApp (Google Spreadsheet API)
  │
  ├─ 呼び出し: processCSVFile(file, ss, sheetName)
  ├─ 呼び出し: processJSONFile(file, ss)
  ├─ 呼び出し: validateImportedData(ss)
  │              └─ 関数呼び出し: DataValidationEnhanced.validateImportedDataEnhanced()
  │
  └─ ファイル依存: Phase 1-6 CSVファイル (11ファイル)


DataValidationEnhanced.gs
  │
  ├─ 依存: SpreadsheetApp (Google Spreadsheet API)
  │
  └─ 検証関数群 (独立実行可能):
       ├─ validateDataTypes(sheet, sheetName)
       ├─ validateCoordinates(sheet)
       ├─ validateColumnCount(sheet, sheetName)
       ├─ detectDuplicateKeys(sheet, keyColumn, sheetName)
       ├─ validateAggregation(ss)
       ├─ validateForeignKeys(ss)
       ├─ validateWardLevelGranularity(sheet)
       └─ validateImportedDataEnhanced(ss) (統合実行)


PersonaDifficultyChecker.gs
  │
  ├─ 依存: SpreadsheetApp (Google Spreadsheet API)
  ├─ 依存: HtmlService (HTML UI表示)
  │
  ├─ 呼び出し: showPersonaDifficultyChecker()
  │              └─ HTML表示: PersonaDifficultyCheckerUI.html
  │
  ├─ データソース: PersonaSummary シート
  │
  └─ 計算関数群:
       ├─ getPersonaDataForDifficulty()
       ├─ calculateDifficultyScore(params)
       ├─ getAgeScore(avgAge)
       ├─ getQualificationLevel(avgQualifications)
       ├─ getMobilityLevel(avgDesiredLocations)
       ├─ getGenderCategory(femaleRatio)
       ├─ getMarketSizeCategory(percentage)
       ├─ getDifficultyLevel(score)
       └─ filterPersonasByConditions(filters)
```

---

### HTMLファイル依存

```
Upload_Enhanced.html
  │
  ├─ 呼び出し元: MenuIntegration.showEnhancedUploadDialog()
  ├─ 依存: HtmlService (Google Apps Script HTML Service)
  │
  └─ 機能: 生CSVファイルアップロードUI


PersonaDifficultyCheckerUI.html
  │
  ├─ 呼び出し元: PersonaDifficultyChecker.showPersonaDifficultyChecker()
  ├─ 依存: HtmlService (Google Apps Script HTML Service)
  │
  ├─ データ取得: PersonaDifficultyChecker.getPersonaDataForDifficulty()
  ├─ フィルタ実行: PersonaDifficultyChecker.filterPersonasByConditions(filters)
  │
  └─ 機能: ペルソナ難易度分析UI（6軸フィルタ）
```

---

## 📊 データフロー詳細

### 1. Python処理フロー

```
[ユーザー] → GUI選択 (tkinter.filedialog)
    ↓
[run_complete.py:main()]
    ↓
入力CSVファイル読み込み (data/input/)
    ↓
[AdvancedJobSeekerAnalyzer:__init__()]
    ├─ load_geocache() → geocache.json読み込み
    ├─ CSVデータ読み込み (pandas.read_csv)
    └─ process_data() 実行
         ├─ _extract_basic_info() → Applicantsデータ生成
         ├─ _process_desired_work() → DesiredWorkデータ生成
         └─ _analyze_flow() → Flowデータ生成
    ↓
[export_phase1_data()]
    ├─ MapMetrics.csv 生成 (❌ データ生成失敗の可能性)
    ├─ Applicants.csv 生成 (✅ 成功)
    ├─ DesiredWork.csv 生成 (❌ データ生成失敗の可能性)
    └─ AggDesired.csv 生成 (✅ 成功)
    ↓
[export_phase2_data()]
    ├─ ChiSquareTests.csv 生成 (❌ データ生成失敗の可能性)
    └─ ANOVATests.csv 生成 (✅ 成功、11カラム)
    ↓
[export_phase3_data(n_clusters=5)]
    ├─ PersonaSummary.csv 生成 (✅ 成功)
    └─ PersonaDetails.csv 生成 (✅ 成功、5カラム)
    ↓
[export_phase6_data()]
    ├─ MunicipalityFlowEdges.csv 生成 (✅ 成功)
    ├─ MunicipalityFlowNodes.csv 生成 (✅ 成功、5カラム)
    └─ ProximityAnalysis.csv 生成 (✅ 成功)
    ↓
[完了] → data/output/phase1-6/ にCSVファイル生成
```

---

### 2. GASインポートフロー

```
[ユーザー] → メニュー選択「📥 Python結果CSVを取り込み」
    ↓
[MenuIntegration.importPythonCSVDialog()]
    ↓
[PythonCSVImporter.batchImportPythonResults()]
    ├─ DriveApp: ファイル検索 (gas_output_phase1/ 等)
    │
    ├─ Phase 1ファイル処理:
    │    ├─ processCSVFile(MapMetrics.csv, ss, "MapMetrics")
    │    ├─ processCSVFile(Applicants.csv, ss, "Applicants")
    │    ├─ processCSVFile(DesiredWork.csv, ss, "DesiredWork")
    │    └─ processCSVFile(AggDesired.csv, ss, "AggDesired")
    │
    ├─ Phase 2ファイル処理:
    │    ├─ processCSVFile(ChiSquareTests.csv, ss, "ChiSquareTests")
    │    └─ processCSVFile(ANOVATests.csv, ss, "ANOVATests")
    │
    ├─ Phase 3ファイル処理:
    │    ├─ processCSVFile(PersonaSummary.csv, ss, "PersonaSummary")
    │    └─ processCSVFile(PersonaDetails.csv, ss, "PersonaDetails")
    │
    ├─ Phase 6ファイル処理:
    │    ├─ processCSVFile(MunicipalityFlowEdges.csv, ss, "MunicipalityFlowEdges")
    │    ├─ processCSVFile(MunicipalityFlowNodes.csv, ss, "MunicipalityFlowNodes")
    │    └─ processCSVFile(ProximityAnalysis.csv, ss, "ProximityAnalysis")
    │
    └─ processJSONFile(gas_map_data.json, ss) (オプション)
    ↓
[validateImportedData(ss)]
    └─ DataValidationEnhanced.validateImportedDataEnhanced()
         ├─ validateDataTypes()
         ├─ validateCoordinates()
         ├─ validateColumnCount()
         ├─ detectDuplicateKeys()
         ├─ validateAggregation()
         ├─ validateForeignKeys()
         └─ validateWardLevelGranularity()
    ↓
[完了] → Googleスプレッドシートに11シート作成
```

---

### 3. ペルソナ難易度分析フロー

```
[ユーザー] → メニュー選択「🎯 ペルソナ難易度確認（NEW）」
    ↓
[PersonaDifficultyChecker.showPersonaDifficultyChecker()]
    ↓
[PersonaDifficultyCheckerUI.html表示]
    ├─ getPersonaDataForDifficulty() 呼び出し
    │    └─ PersonaSummaryシートからデータ取得
    │
    ├─ ユーザー操作: 6軸フィルタ選択
    │    ├─ 年齢グループ
    │    ├─ 性別カテゴリ
    │    ├─ 資格レベル
    │    ├─ 移動性レベル
    │    ├─ 市場規模
    │    └─ 総合難易度
    │
    └─ filterPersonasByConditions(filters) 呼び出し
         └─ calculateDifficultyScore() による難易度計算
              ├─ getAgeScore()
              ├─ getQualificationLevel()
              ├─ getMobilityLevel()
              ├─ getGenderCategory()
              ├─ getMarketSizeCategory()
              └─ getDifficultyLevel()
    ↓
[UI更新] → フィルタ結果のペルソナ一覧表示
```

---

## 🏗️ アーキテクチャパターン

### レイヤー構造

```
┌────────────────────────────────────────┐
│  プレゼンテーション層                   │
│  (Googleスプレッドシート + HTML UI)     │
│  - メニューUI                          │
│  - Upload_Enhanced.html                │
│  - PersonaDifficultyCheckerUI.html     │
└─────────────┬──────────────────────────┘
              │
              ▼
┌────────────────────────────────────────┐
│  ビジネスロジック層                     │
│  (Google Apps Script)                  │
│  - MenuIntegration.gs                  │
│  - PythonCSVImporter.gs                │
│  - DataValidationEnhanced.gs           │
│  - PersonaDifficultyChecker.gs         │
└─────────────┬──────────────────────────┘
              │
              ▼
┌────────────────────────────────────────┐
│  データ処理層                          │
│  (Pythonスクリプト)                    │
│  - run_complete.py                     │
│  - test_phase6_temp.py                 │
│  - COMPREHENSIVE_TEST_SUITE.py         │
└─────────────┬──────────────────────────┘
              │
              ▼
┌────────────────────────────────────────┐
│  データ永続化層                        │
│  (ファイルシステム)                    │
│  - data/input/ (入力CSV)               │
│  - data/output/ (出力CSV + JSON)       │
│  - geocache.json (キャッシュ)          │
└────────────────────────────────────────┘
```

---

### デザインパターン

#### 1. **Facade パターン**

**適用箇所**: `run_complete.py`

**目的**: 複雑なPhase 1-6処理を単一インターフェースで提供

```python
# 複雑な内部処理を隠蔽
analyzer = AdvancedJobSeekerAnalyzer(filepath)
analyzer.export_phase1_data()  # 内部で複数メソッド実行
analyzer.export_phase2_data()
analyzer.export_phase3_data()
analyzer.export_phase6_data()
```

---

#### 2. **Cache パターン**

**適用箇所**: `geocache.json`

**目的**: Google Maps API呼び出し削減

```python
def get_coordinates(address, cache, api_client):
    if address in cache:
        return cache[address]  # キャッシュヒット

    # キャッシュミス → API呼び出し
    result = api_client.geocode(address)
    cache[address] = result
    save_geocache(cache)
    return result
```

---

#### 3. **Strategy パターン**

**適用箇所**: Phase別エクスポートメソッド

**目的**: Phase別の異なる処理アルゴリズムをカプセル化

```python
class AdvancedJobSeekerAnalyzer:
    def export_phase1_data(self, output_dir):
        # Phase 1専用アルゴリズム
        pass

    def export_phase2_data(self, output_dir):
        # Phase 2専用アルゴリズム
        pass

    def export_phase3_data(self, output_dir, n_clusters):
        # Phase 3専用アルゴリズム
        pass

    def export_phase6_data(self, output_dir):
        # Phase 6専用アルゴリズム
        pass
```

---

#### 4. **Validator パターン**

**適用箇所**: `DataValidationEnhanced.gs`

**目的**: 7種類の独立した検証ルールを組み合わせ

```javascript
function validateImportedDataEnhanced(ss) {
    var results = [];

    // 各検証を独立実行
    results.push(validateDataTypes(sheet, sheetName));
    results.push(validateCoordinates(sheet));
    results.push(validateColumnCount(sheet, sheetName));
    results.push(detectDuplicateKeys(sheet, keyColumn, sheetName));
    results.push(validateAggregation(ss));
    results.push(validateForeignKeys(ss));
    results.push(validateWardLevelGranularity(sheet));

    // 統合評価
    return calculateScore(results);
}
```

---

## 🔌 外部依存関係

### Pythonライブラリ

```
pandas (データ処理)
  └─ 用途: CSV読み込み、DataFrame操作、集計

numpy (数値計算)
  └─ 用途: 統計計算、配列操作

matplotlib (グラフ描画)
  └─ 用途: 拡張分析グラフ生成

scikit-learn (機械学習)
  └─ 用途: ペルソナクラスタリング (n_clusters=5)

googlemaps (ジオコーディング)
  └─ 用途: 住所→座標変換 (キャッシュ併用)
  └─ 依存: Google Maps Geocoding API

tkinter (GUI)
  └─ 用途: ファイル選択ダイアログ

json, os, re (標準ライブラリ)
  └─ 用途: ファイル操作、正規表現処理
```

---

### Google APIs

```
Google Apps Script
  ├─ SpreadsheetApp (スプレッドシート操作)
  ├─ DriveApp (ファイル検索・読み込み)
  ├─ HtmlService (HTML UI表示)
  └─ Utilities (ユーティリティ関数)

Google Maps Geocoding API (オプション)
  └─ 用途: 住所→座標変換 (geocache.json優先使用)
```

---

## 🔒 セキュリティ・制約事項

### API制限

**Google Maps Geocoding API**:
- 無料枠: 月間$200分 (約40,000リクエスト)
- 現状: geocache.json (1,901件) により大幅削減
- 対策: キャッシュ優先、新規住所のみAPI呼び出し

**Google Apps Script**:
- 実行時間制限: 6分/実行
- メモリ制限: 100MB
- 対策: 大容量CSVは分割インポート推奨

---

### ファイルシステム制約

**Windows環境**:
- パス長制限: 260文字
- 現状: 問題なし（最長パス < 150文字）

**エンコーディング**:
- Python出力: UTF-8 with BOM
- GAS読み込み: UTF-8対応
- Excel互換: BOM使用により文字化け防止

---

## 📂 ディレクトリ構造と役割

```
job_medley_project/
├─ python_scripts/              # Python実行スクリプト
│   ├─ run_complete.py          # エントリーポイント
│   ├─ test_phase6_temp.py      # 分析エンジン (ライブラリ)
│   └─ COMPREHENSIVE_TEST_SUITE.py  # MECEテストスイート
│
├─ gas_files/                   # GASファイル (コピペ用)
│   ├─ scripts/                 # .gsファイル
│   │   ├─ MenuIntegration.gs
│   │   ├─ PythonCSVImporter.gs
│   │   ├─ DataValidationEnhanced.gs
│   │   └─ PersonaDifficultyChecker.gs
│   └─ html/                    # .htmlファイル
│       ├─ Upload_Enhanced.html
│       └─ PersonaDifficultyCheckerUI.html
│
├─ data/                        # データファイル
│   ├─ input/                   # 入力CSVファイル
│   │   ├─ 統合_求職者情報沖縄_介護 (1).csv
│   │   ├─ 統合_求職者情報京都_介護 (1).csv
│   │   ├─ 栃木_生活相談員 (1).csv
│   │   └─ 統合_求職者情報宮城_介護.csv
│   └─ output/                  # 出力CSVファイル
│       ├─ phase1/
│       ├─ phase2/
│       ├─ phase3/
│       ├─ phase6/
│       └─ geocache.json
│
└─ docs/                        # MECEドキュメント
    ├─ CODE_REFERENCE.md        # コードリファレンス
    ├─ DATA_SPECIFICATION.md    # データ仕様書
    ├─ ARCHITECTURE.md          # このファイル
    └─ PHASE_COMPLETE_MAP.md    # Phase完全マップ
```

---

## 🔄 実行シーケンス

### 初回セットアップ

```
1. [ユーザー] 入力CSVファイルを data/input/ に配置
    ↓
2. [Python] run_complete.py 実行
    ├─ GUIでdata/input/ファイルを選択
    ├─ Phase 1-6処理実行
    └─ data/output/ にCSV生成
    ↓
3. [GAS] GASファイルをApps Scriptプロジェクトにコピペ
    ├─ 4つの.gsファイル
    └─ 2つの.htmlファイル
    ↓
4. [GAS] スプレッドシートでメニュー「📥 Python結果CSVを取り込み」
    ├─ 11ファイルインポート
    └─ データ検証実行
    ↓
5. [完了] Googleスプレッドシートで分析機能使用可能
```

---

### 定常運用

```
1. [ユーザー] 新規データ追加
    ↓
2. [選択肢A] GASから直接アップロード
    └─ メニュー「⚡ 高速CSVインポート（推奨）」
         └─ Upload_Enhanced.html でアップロード
    ↓
   [選択肢B] Python処理後にGASインポート
    └─ run_complete.py 実行
         └─ メニュー「📥 Python結果CSVを取り込み」
    ↓
3. [GAS] 各種分析機能使用
    ├─ 地図表示
    ├─ 統計分析
    ├─ ペルソナ難易度確認
    └─ フロー・移動パターン分析
```

---

## 📝 アーキテクチャ上の課題

### 既知の問題

1. **データ生成失敗**
   - MapMetrics.csv, DesiredWork.csv, ChiSquareTests.csv が空
   - 原因: 不明（要調査）
   - 影響: 地図表示、統計分析機能が動作不可

2. **test_phase6_temp.py命名矛盾**
   - ファイル名: Phase 6専用を示唆
   - 実態: Phase 1, 2, 3, 6すべて担当
   - 影響: 可読性低下（機能には影響なし）

3. **入力CSVカラム名の非セマンティック性**
   - カラム名: HTMLクラス名（例: u-fw-bold）
   - 影響: データマッピングがコード内部に隠蔽
   - 対処: 入力データ仕様の文書化が必要

4. **Phase 4, 5の不在**
   - 設計上存在しないPhase
   - 理由: 不明（要プロジェクト設計書確認）

---

### 改善提案

1. **エラーハンドリング強化**
   - 空CSVファイル生成時に警告出力
   - データ生成失敗の原因調査機能追加

2. **ファイル命名規則の見直し**
   - `test_phase6_temp.py` → `job_medley_analyzer.py`
   - 役割を明確にする命名に変更

3. **入力データバリデーション**
   - 入力CSVカラム数・カラム名検証
   - 期待データ構造との不一致検出

4. **ドキュメント自動生成**
   - コード変更時にドキュメント更新を自動化
   - データ仕様とコードの同期維持

---

## 📅 メンテナンス情報

- **作成日**: 2025-10-26
- **最終更新**: 2025-10-26
- **ドキュメント種別**: システムアーキテクチャ（MECE準拠）
- **対象バージョン**: 1.0
