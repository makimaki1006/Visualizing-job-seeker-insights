# Phase 12-14 MAP統合ガイド

**作成日**: 2025年11月2日
**バージョン**: 1.0
**対応Phase**: Phase 12（需給ギャップ分析）、Phase 13（希少性スコア）、Phase 14（競合分析）

---

## 📋 目次

1. [概要](#概要)
2. [前提条件](#前提条件)
3. [統合手順](#統合手順)
4. [データ構造](#データ構造)
5. [カスタマイズ](#カスタマイズ)
6. [トラブルシューティング](#トラブルシューティング)

---

## 📊 概要

このガイドでは、Phase 12-14で生成されたCSVデータを`map_complete_integrated.html`に統合し、既存のMAP可視化システムに組み込む方法を説明します。

### 追加される機能

#### Phase 12: 需給ギャップ分析タブ

- **KPI**: 総分析地域数、総需要、総供給、平均需給比率
- **テーブル**:
  - 需給ギャップTop 10（需要 - 供給）
  - 需給比率Top 10
- **グラフ**:
  - Top 10 需給ギャップ（横棒グラフ）
  - Top 10 需給比率（横棒グラフ）

#### Phase 13: 希少性スコアタブ

- **KPI**: 総組み合わせ数、超希少（S）件数、非常に希少（A）件数、希少（B）件数
- **テーブル**:
  - 希少性ランク分布
  - 希少性スコアTop 15
- **グラフ**:
  - 希少性ランク分布（ドーナツチャート）
  - Top 10 希少性スコア（横棒グラフ）

#### Phase 14: 競合分析タブ

- **KPI**: 総分析地域数、総応募者数、平均女性比率、平均国家資格保有率
- **テーブル**:
  - 競争が激しい地域Top 15
- **グラフ**:
  - Top 10 総応募者数（横棒グラフ）
  - Top 10 女性比率（横棒グラフ）

---

## 🔧 前提条件

### 1. Pythonスクリプトの実行

Phase 12-14のCSVファイルを生成する必要があります。

```bash
cd "C:\Users\fuji1\OneDrive\Pythonスクリプト保管\job_medley_project\python_scripts"
python run_complete_v2_perfect.py
```

**出力先**:
- `data/output_v2/phase12/SupplyDemandGap.csv` (1,528件)
- `data/output_v2/phase13/RarityScore.csv` (4,500件)
- `data/output_v2/phase14/CompetitionProfile.csv` (1,032件)

### 2. GASプロジェクトの準備

以下のファイルがGASプロジェクトに存在する必要があります：

- `map_complete_integrated.html` (更新版)
- `MapPhase12_14_DataBridge.gs` (新規)

---

## 🚀 統合手順

### ステップ1: CSVファイルをGoogleスプレッドシートにインポート

1. Googleスプレッドシートを開く
2. 以下の3つのCSVファイルをインポート：
   - `SupplyDemandGap.csv` → シート名: `Phase12_SupplyDemandGap`
   - `RarityScore.csv` → シート名: `Phase13_RarityScore`
   - `CompetitionProfile.csv` → シート名: `Phase14_CompetitionProfile`

**インポート方法**（高速CSVアップロード推奨）:
```
GASメニュー: データ処理 → ⚡ 高速CSVインポート（推奨）
```

または手動インポート:
```
ファイル → インポート → アップロード → ファイルを選択
インポート場所: 新しいシートに挿入
```

### ステップ2: GASスクリプトをプロジェクトに追加

1. GASエディタを開く（拡張機能 → Apps Script）
2. 新しいファイルを作成: `MapPhase12_14_DataBridge.gs`
3. `C:\Users\fuji1\OneDrive\Pythonスクリプト保管\job_medley_project\gas_files\scripts\MapPhase12_14_DataBridge.gs`の内容をコピー＆ペースト
4. 保存

### ステップ3: HTMLファイルを更新

1. GASプロジェクトの`map_complete_integrated.html`を開く
2. 更新版の内容に置き換える
   - タブ定義にPhase 12-14を追加（TABS配列）
   - パネルHTMLにgap, rarity, competitionを追加
   - renderGap(), renderRarity(), renderCompetition()関数を追加
   - renderCity()にPhase 12-14のrender呼び出しを追加
3. 保存

### ステップ4: データ連携関数の統合

既存の`getMapCompleteData()`関数に Phase 12-14データを統合します。

**方法A: 既存関数を拡張**

```javascript
function getMapCompleteData() {
  // 既存のPhase 1-10データ取得処理
  const citiesData = [...]; // 既存コード

  // Phase 12-14データをロード
  const phase12to14Data = loadPhase12to14Data();

  // 各都市データにPhase 12-14を追加
  citiesData.forEach(city => {
    city.gap = phase12to14Data.gap;
    city.rarity = phase12to14Data.rarity;
    city.competition = phase12to14Data.competition;
  });

  return citiesData;
}
```

**方法B: 新しい関数を使用**

```javascript
// MapPhase12_14_DataBridge.gs
function getMapCompleteDataWithPhase12to14() {
  const citiesData = getMapCompleteData(); // 既存関数を呼び出し
  const phase12to14Data = loadPhase12to14Data();

  citiesData.forEach(city => {
    city.gap = phase12to14Data.gap;
    city.rarity = phase12to14Data.rarity;
    city.competition = phase12to14Data.competition;
  });

  return citiesData;
}
```

### ステップ5: メニューに追加（オプション）

GASメニューにPhase 12-14統合版のMAPを追加します。

```javascript
function onOpen() {
  const ui = SpreadsheetApp.getUi();
  ui.createMenu('📊 データ処理')
    // ... 既存メニュー ...
    .addSeparator()
    .addItem('🗺️ MAP分析（Phase 12-14統合版）', 'showMapPhase12to14')
    .addToUi();
}
```

### ステップ6: テスト実行

1. GASエディタで`testPhase12to14Load()`関数を実行
2. ログを確認してデータが正しくロードされているか確認

```
実行 → testPhase12to14Load
表示 → ログ
```

**期待される出力**:
```
=== Phase 12: 需給ギャップ分析 ===
総地域数: 1528
Top 5 ギャップ:
  1. 東京都世田谷区: 需要=1281, 供給=0, ギャップ=1281
  2. 東京都練馬区: 需要=1259, 供給=0, ギャップ=1259
  ...

=== Phase 13: 希少性スコア ===
総組み合わせ数: 4500
S rank: 2205件
Top 5 希少性:
  1. 三重県松阪市 30代 女性: スコア=1.0
  ...

=== Phase 14: 競合分析 ===
総地域数: 1032
Top 5 競争激しい地域:
  1. 東京都世田谷区: 総応募者数=1281, 女性比率=72.4%
  ...
```

### ステップ7: MAP統合版を表示

1. スプレッドシートのメニューから実行:
   ```
   📊 データ処理 → 🗺️ MAP分析（Phase 12-14統合版）
   ```

2. または、直接関数を実行:
   ```javascript
   showMapPhase12to14();
   ```

---

## 📊 データ構造

### cityオブジェクトの拡張

```javascript
{
  id: "kyoto-fushimi",
  name: "京都府 京都市伏見区",
  center: [34.935, 135.76],
  updated: "2025-10-31",
  overview: {...}, // 既存
  supply: {...}, // 既存
  career: {...}, // 既存
  urgency: {...}, // 既存
  persona: {...}, // 既存
  cross: {...}, // 既存
  flow: {...}, // 既存

  // ========== 新規追加 ==========

  gap: { // Phase 12: 需給ギャップ分析
    top_gaps: [
      {
        location: "東京都世田谷区",
        demand_count: 1281,
        supply_count: 0,
        gap: 1281,
        demand_supply_ratio: 1281.0,
        latitude: 35.69,
        longitude: 139.69
      },
      // ... Top 10
    ],
    top_ratios: [
      // ... Top 10
    ],
    summary: {
      total_locations: 1528,
      total_demand: 50000,
      total_supply: 15000,
      avg_ratio: 45.2
    }
  },

  rarity: { // Phase 13: 希少性スコア
    rank_distribution: {
      "S: 超希少（1人のみ）": 2205,
      "A: 非常に希少（2人）": 628,
      "B: 希少（3-5人）": 515,
      "C: やや希少（6-20人）": 631,
      "D: 一般的（20人超）": 521
    },
    top_rarity: [
      {
        location: "三重県松阪市",
        age_bucket: "30代",
        gender: "女性",
        has_national_license: false,
        count: 1,
        rarity_score: 1.0,
        rarity_rank: "S: 超希少（1人のみ）",
        latitude: 34.58,
        longitude: 136.53
      },
      // ... Top 15
    ],
    summary: {
      total_combinations: 4500,
      s_rank: 2205,
      a_rank: 628,
      b_rank: 515
    }
  },

  competition: { // Phase 14: 競合分析
    top_locations: [
      {
        location: "東京都世田谷区",
        total_applicants: 1281,
        top_age_group: "50代",
        top_age_ratio: 0.223,
        female_ratio: 0.724,
        male_ratio: 0.276,
        national_license_rate: 0.007,
        top_employment_status: "就業中",
        top_employment_ratio: 0.560,
        avg_qualification_count: 1.06,
        latitude: 35.69,
        longitude: 139.69
      },
      // ... Top 15
    ],
    summary: {
      total_locations: 1032,
      total_applicants: 80000,
      avg_female_ratio: 0.73,
      avg_national_license_rate: 0.01
    }
  }
}
```

---

## 🎨 カスタマイズ

### タブの表示順を変更

`map_complete_integrated.html`の`TABS`配列を編集:

```javascript
const TABS = [
  {id:'overview',label:'総合概要'},
  {id:'gap',label:'需給ギャップ'}, // 前に移動
  {id:'supply',label:'人材供給'},
  // ...
];
```

### Top N件数を変更

各render関数内の`.slice(0, 10)`部分を変更:

```javascript
// Top 15に変更する場合
const topGaps = (g.top_gaps||[]).slice(0, 15);
```

### GASのシート名を変更

`MapPhase12_14_DataBridge.gs`の関数呼び出し時に引数を指定:

```javascript
const data = loadPhase12to14Data(
  'カスタムPhase12シート名',
  'カスタムPhase13シート名',
  'カスタムPhase14シート名'
);
```

---

## 🛠️ トラブルシューティング

### 問題1: タブが表示されない

**原因**: HTMLファイルが正しく更新されていない

**解決方法**:
1. GASプロジェクトで`map_complete_integrated.html`を開く
2. TABS配列にPhase 12-14が追加されているか確認
3. 保存して再読み込み

### 問題2: データが表示されない

**原因**: CSVファイルがシートにインポートされていない、またはシート名が一致しない

**解決方法**:
1. スプレッドシートで以下のシートが存在するか確認:
   - `Phase12_SupplyDemandGap`
   - `Phase13_RarityScore`
   - `Phase14_CompetitionProfile`
2. シート名が一致しない場合は、`loadPhase12to14Data()`の引数で指定

### 問題3: グラフが表示されない

**原因**: Chart.jsの初期化エラー、またはデータが空

**解決方法**:
1. ブラウザの開発者ツール（F12）を開く
2. コンソールにエラーが表示されていないか確認
3. `testPhase12to14Load()`を実行してデータが正しくロードされているか確認

### 問題4: 座標データが表示されない

**原因**: latitude/longitude列が空、またはgeocache.jsonに座標データがない

**解決方法**:
1. Pythonスクリプトを再実行（geocache.jsonが更新されます）
2. CSVファイルのlatitude/longitude列を確認
3. 必要に応じて、Pythonコードで座標データのマッピングを修正

---

## 📖 関連ドキュメント

- **Python実装**: `python_scripts/run_complete_v2_perfect.py` (Phase 12-14実装)
- **テストスクリプト**: `python_scripts/test_phase12_13_14.py`
- **GAS連携**: `gas_files/scripts/MapPhase12_14_DataBridge.gs`
- **HTMLファイル**: `gas_files/html/map_complete_integrated.html`

---

## ✅ 完了チェックリスト

- [ ] Python実行でPhase 12-14のCSVファイル生成完了
- [ ] 3つのCSVファイルをGoogleスプレッドシートにインポート完了
- [ ] `MapPhase12_14_DataBridge.gs`をGASプロジェクトに追加完了
- [ ] `map_complete_integrated.html`を更新版に置き換え完了
- [ ] `testPhase12to14Load()`関数でデータロード確認完了
- [ ] MAP統合版を表示して、Phase 12-14タブが正しく動作することを確認完了

---

## 🎉 統合完了後の確認

MAP統合版を表示して、以下を確認してください：

1. **タブ**: 11個のタブが表示される（overview, supply, career, urgency, persona, cross, flow, gap, rarity, competition, dashboard）
2. **需給ギャップタブ**: KPI、テーブル、グラフが表示される
3. **希少性スコアタブ**: KPI、ランク分布、テーブル、グラフが表示される
4. **競合分析タブ**: KPI、テーブル、グラフが表示される
5. **データの正確性**: Top N件が正しく表示される

すべて確認できれば、Phase 12-14のMAP統合は完了です！🎊
